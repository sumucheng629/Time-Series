# Workflow Run Notes - 2026-05-31

This note records the first successful full workflow run after the R execution
environment was configured for this repository. It is a reproducibility note,
not a final interpretation of the assignment results.

## Run Context

- Repository path used by Git: `C:\Users\李海毅\OneDrive\My_Research\Time-Series-sumucheng`
- ASCII working path used by R: `C:\ts-sumucheng`
- R executable: `C:\Program Files\R\R-4.4.1\bin\Rscript.exe`
- R library path used for packages: `C:\Rlibs\4.4`
- Temporary directory used during package installation and workflow runs:
  `C:\Rtmp`

The ASCII working path was used because this R session had path-encoding
problems when packages tried to read or write files under the non-ASCII Windows
user directory.

## Command Used

```powershell
$env:R_LIBS_USER = 'C:/Rlibs/4.4'
$env:TEMP = 'C:/Rtmp'
$env:TMP = 'C:/Rtmp'
& 'C:\Program Files\R\R-4.4.1\bin\Rscript.exe' --vanilla -e ".libPaths('C:/Rlibs/4.4'); source('scripts/run_project.R')"
```

## Validation Result

The generated `outputs/tables/data_validation_checks.csv` indicates that:

- both expected measures are present
- no missing observations were found
- no duplicate measure-year keys were found
- yearly observations are consecutive
- all observations are positive
- each selected measure has 66 observations

## Forecast Evaluation Window

The generated `outputs/tables/forecast_design.csv` records:

- full series: 1960-2025
- training window: 1960-2015
- holdout window: 2016-2025

## Generated Outputs

The run generated tables under `outputs/tables/` and figures under
`outputs/figures/`. These files are committed so the report draft can refer to
specific evidence while the workflow remains reproducible from source scripts.

## Notes For Next Work

- Review the generated diagnostics before selecting final models.
- Treat `best_holdout_models.csv` as a guide, not as the sole model-selection
  rule.
- Check whether the final report should use original-scale or log-scale birth
  forecasts.
